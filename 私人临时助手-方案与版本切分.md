# 私人临时助手：方案与版本切分

## 1. 产品定位

这是一个「临时收纳 + 稍后处理 + 知识沉淀」的个人助手 Agent。

核心目标：

- 先接住内容，再延迟决策
- 降低收集成本，减少分类焦虑
- 逐步把高价值内容沉淀为个人知识系统

---

## 2. 端与架构决策（已确认）

- Windows 端：轻量桌面小窗（快捷键唤起，负责高频快收）
- 移动端：PWA/H5（先做，形态接近 App）
- 主页面：Web（Notion 风格管理页）
- 核心能力：统一后端 + 统一 Agent（跨端共享同一内容库和智能能力）

---

## 3. Windows 端（V1）

通过快捷键唤起临时小窗，包含 3 个可切换功能：

1. 截图暂存
- 临时放截图，便于随取随用

2. 复制暂存
- 临时放复制内容
- 每条内容支持一键复制

3. 导入收件箱
- 支持导入文字、链接等内容
- 状态默认值：`稍后看`（用户可改）
- 导入成功后：提示“已导入”并清空输入框

---

## 4. 移动端（V1）

移动端第一版采用 PWA/H5，优先覆盖：

- 链接/文字粘贴导入
- 微信场景承接（先通过 H5 流程跑通）
- 收藏内容导入能力逐步增强

---

## 5. 管理主页面（Notion 风格，V1）

### 5.1 列表字段（已确认）

每条记录展示：

1. 内容
2. 标题（插入在第 2 位）
3. 摘要
4. 时间
5. 标签
6. 批注标记

### 5.2 标题生成规则（已确认）

- 复制文字：取前 `10` 个字
- 链接：优先抓取页面标题
- 普通网站链接（非图文/非视频）：显示网站名

### 5.3 详情页（已确认）

- 每条记录可点击展开详情（类似 Notion 单条页面）
- 支持查看/编辑批注
- 保留“生成笔记”入口

---

## 6. 标签与状态体系（已确认）

### 6.1 状态

三元状态：

- 稍后看
- 看完
- 收藏

新导入内容如果未手动选择状态，默认：`稍后看`。

### 6.2 固定标签

固定常用标签：

- 文字
- 文档
- 小红书
- B站

### 6.3 自定义标签

- 支持用户通过 `+` 新增自定义标签
- 支持多标签并存（例如：`B站 + 学习`）

### 6.4 AI 标签判别规则

- 保存时用户可手动选固定标签
- 若用户未选固定标签，AI 自动判别
- 若均不匹配，归类到 `其他`
- 未筛选时，`其他` 也正常展示

### 6.5 状态筛选交互（已确认）

- 提供 `全部` 按钮
- 当 `全部` 选中时：三状态按钮均不选中（但显示全量数据）
- 当任一状态按钮被选中时：`全部` 自动取消
- 支持状态多选筛选

---

## 7. Agent 能力（V1）

## 7.1 摘要策略

- `60` 字以内短文本：不摘要
- 中长文本：自动短摘要
- 链接：先抓标题/描述，抓到正文再摘要

### 7.2 生成笔记

- 仅在用户点击“生成笔记”时触发（不自动触发）
- 目标：基于已存内容整理为可复用知识

### 7.3 抓取失败策略（硬规则）

- 抓取不到内容时：必须判定失败原因，不强行写
- 返回失败原因（如权限、登录限制、链接失效、超时等）
- 保留原链接与已有信息
- `30` 分钟后自动重试 `1` 次
- 重试仍失败则保持失败状态

### 7.4 状态联动

- 生成笔记成功后，不自动修改状态（由用户手动改）

---

## 8. 对话式搜索（V1）

搜索主形态：客服式对话检索。

流程：

1. 用户在对话框输入想法
2. Agent 理解意图并检索历史记录
3. 返回最多 `5` 条最相关结果卡片
4. 点击卡片展开详情

结果不足时：

- 自动放宽条件再检索一次
- 若仍无结果：提示“没有找到”，并给改写建议

命中结果时：

- Agent 基于命中记录输出总结与建议
- 链接不实时外读，只基于系统内已有信息（标签、批注、摘要、笔记）回答

---

## 9. 每日提醒（V1）

默认提醒时间：每天 `11:00`（可修改）

提醒内容：

1. 今日稍后看数量
2. 最近 3 天累计稍后看数量
3. 今日看完数量
4. 今日收藏数量
5. 今日关注领域（按导入内容计算）

---

## 10. 版本切分（已更新）

## V1（先上线可用）

- 完成当前文档中的核心闭环能力
- 不做收藏夹
- 不做复杂微信深度接入

## V1.1（体验增强）

- 微信入口增强
- 搜索相关性优化与回顾增强
- 笔记质量优化与提醒体验优化
- 主题合集自动生成（3-8 条同主题记录合并）
- 知识连接建议（与历史记录的关联提示）
- 学习路径建议（今天/本周建议处理顺序）
- 概念卡片初版（定义 + 场景 + 例子）
- 不做 Windows 多分组/多会话临时区

## V2（知识系统化）

- 收藏夹系统
- 平台收藏导入自动化增强
- 更强知识归纳与主题洞察
- 主题地图可视化（知识网络）
- 冲突与空白检测（矛盾观点、缺失环节）
- 内化检查（反思题/应用题）
- 复用输出（分享稿、方案草稿、行动清单）

---

## 11. 当前关键约束总结

- 第一版保持轻量，不引入过重结构
- 收藏夹延后到 V2
- Windows 多分组/多会话临时区不进入 V1.1
- 搜索采用对话式为主，传统筛选为辅

---

## 12. 成长教练 Agent（新增）

定位：不仅管理信息，更要推动用户完成“接收 -> 理解 -> 内化 -> 复用”。

### 12.1 V1 必做能力

1. 今日建议处理 3 条（不只提示数量）
2. 相似内容识别（提示可合并学习）
3. 记录内问答可一键存入笔记
4. 今日关注领域（按导入内容统计）

### 12.2 V1.1 增强能力

1. 主题合集自动生成（3-8 条）
2. 知识连接建议（新旧内容关联）
3. 学习路径建议（今日/本周）
4. 概念卡片初版

### 12.3 V2 深化能力

1. 主题地图可视化
2. 冲突与空白检测
3. 内化检查机制
4. 可复用输出自动生成

---

## 13. 视频读取与转录规则（新增）

### 13.1 触发条件（硬规则）

仅在以下两种情况下触发读取：

1. 用户点击“生成笔记”
2. 用户明确要求读取某条记录中的视频

### 13.2 处理流程

1. 提取音频
2. 本地 Whisper 语音转录
3. 保存转录文本
4. 自动总结要点

### 13.3 持久化与复用

只要触发过一次读取，就把“转录文本 + 要点总结”存回该记录，后续优先复用，避免重复转录成本。
