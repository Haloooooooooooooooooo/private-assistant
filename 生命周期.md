# 生命周期

**如果一开始就设计成 6 个状态，确实会太复杂，也很难维护。**

尤其你的用户本来就是因为“不想整理、不想维护、收藏夹吃灰”才需要这个产品。
如果你再让用户维护一堆状态，本质上只是把 Notion/Obsidian 的复杂性换了一种形式。

所以生命周期要简化。

------

# 一、不要让用户维护生命周期

你的原则应该是：

> **状态是 Agent 用来管理内容的，不是让用户手动维护的。**

用户只需要做很少的动作：

```markdown
丢进去
看一眼
保留 / 不保留
需要时提问
```

状态判断、整理、提醒、归类，尽量都由 Agent 自动完成。

------

# 二、一开始只保留 3 个状态就够了

建议你先用这 3 个状态：

```markdown
1. 暂存
2. 待内化
3. 已沉淀
```

这比之前的生命周期更轻。

------

## 1. 暂存

意思是：

> 我只是先丢进来，还没判断它值不值得长期保存。

适合：

```markdown
临时截图
待会要看的链接
随手复制的一段话
还没来得及判断价值的内容
```

Agent 要做的事：

```markdown
自动生成标题
识别内容类型
判断可能主题
提醒我是否要处理
```

用户只需要决定：

```markdown
保留
删除
稍后看
```

------

## 2. 待内化

意思是：

> 这条内容有价值，但还没有真正变成我的知识。

适合：

```markdown
我看完觉得有用的文章
我收藏的干货
我自己的感想
我想以后复用的观点
值得整理进知识体系的内容
```

Agent 要做的事：

```markdown
提炼核心观点
整理成可复用笔记
补充我的理解
关联旧记录
建议归入哪个主题
```

用户只需要做：

```markdown
确认
修改
补一句自己的理解
```

------

## 3. 已沉淀

意思是：

> 这条内容已经整理过，可以被以后调用。

适合：

```markdown
已经整理成笔记的内容
已经归入某个主题的内容
已经能用于问答、写作、面试、产品设计的内容
```

Agent 要做的事：

```markdown
参与知识库问答
在相关问题中被召回
定期进入回顾
和新内容建立连接
```

用户不需要经常维护。

------

# 三、简化后的内容流转

可以变成这样：

```markdown
丢进来 → 暂存
觉得有价值 → 待内化
整理完成 → 已沉淀
没价值 / 过期 → 删除或归档
```

这就够了。

不需要一开始就做：

```markdown
待处理
已整理
待回顾
已内化
可复用
可归档
```

这些状态听起来专业，但对普通用户来说会有负担。

------

# 四、提醒也不要基于复杂状态

你每天提醒用户时，不要说：

```markdown
你有 3 条待处理、2 条待回顾、4 条可归档内容
```

这会让用户有压力。

可以改成：

```markdown
今天建议你处理 3 条内容：
1. 一条暂存内容，判断是否保留
2. 一条待内化内容，补一句你的理解
3. 一条已沉淀内容，看看现在还能不能用
```

这样用户感知到的是“轻任务”，不是“维护系统”。

------

# 五、状态可以对用户隐藏一部分

你可以把状态分成两层：

## 用户可见状态

只给用户看简单的：

```markdown
暂存
待内化
已沉淀
```

## 系统内部状态

Agent 内部可以更细，比如：

```markdown
是否已读
是否已提炼
是否已关联
是否已复习
是否可调用
是否过期
```

但这些不要全部展示给用户。

也就是说：

> **复杂性可以存在于系统内部，但不要暴露给用户。**

------

# 六、你真正要设计的不是“生命周期”，而是“下一步动作”

对用户来说，状态其实没那么重要。
更重要的是：

```markdown
我现在该对这条内容做什么？
```

所以你可以把状态弱化，把重点放在按钮或建议动作上。

例如每条内容下面只出现：

```markdown
整理成笔记
加入知识库
稍后提醒
删除
```

或者 Agent 直接给建议：

```markdown
这条更适合沉淀为“Agent 评测方法”笔记，是否整理？
```

这样比让用户理解生命周期更自然。

------

# 七、最推荐的设计方式

我建议你这样设计：

```markdown
前台：用户看到的是任务和建议
后台：系统维护状态和知识流转
```

也就是：

## 用户看到

```markdown
今天建议处理：
- 这条链接值得整理成笔记
- 这张截图可能只是临时内容，是否删除？
- 这条想法可以归入“Agent 产品设计”
```

## 系统实际维护

```markdown
暂存
待内化
已沉淀
已归档
已删除
```

用户不需要知道太多状态。

------

# 八、最终建议

你一开始不要做复杂生命周期。

最小版本只需要：

```markdown
暂存
待内化
已沉淀
```

再配两个动作：

```markdown
删除
归档
```

这样就够了。

你的产品不是要让用户“管理状态”，而是要让用户觉得：

> **我只要丢进去，Agent 会告诉我下一步怎么处理。**

所以更好的产品表达不是：

```markdown
知识生命周期管理
```

而是：

```markdown
每条内容都有一个清晰的下一步。
```

这才符合你的产品定位：
**降低整理成本，而不是增加维护成本。**